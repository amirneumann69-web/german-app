# نقشه راه قدم به قدم - اپلیکیشن آلمانی با داستان

### هدف نهایی: انتشار در کافه‌بازار + کسب درآمد از اشتراک

---

### ✅ مرحله ۱: طراحی (الان اینجاییم)
**کاری که تو باید بکنی:**
تو داری روی طراحی کار می‌کنی - عالیه. می‌تونی:
- روی کاغذ بکشی و عکسش رو به من بدی
- یا با Figma / یا حتی با توضیح نوشتاری بگی: "صفحه اول این شکلی باشه، این دکمه اینجا باشه"
- هر وقت آماده شد به من بده تا من دقیقا همون رو به کد فلاتر تبدیل کنم.

**ساختار استاندارد اپ ما (همینی که خودت گفتی):**

```
[صفحه 1: خانه]
   ↓ کلیک روی A1
[صفحه 2: لیست درس‌های A1] -> 12 تا داستان
   ↓ کلیک روی داستان 1
[صفحه 3: صفحه آموزش] -> داستان آلمانی + دکمه ترجمه مخفی + لغات + گرامر + تمرین
```

**نقشه اپ ما:**
1.  Splash Screen (لوگوی اپ)
2.  Home Screen: نمایش 6 سطح (A1 رایگان با تیک سبز، A2 تا C2 قفل با آیکون قفل و دکمه خرید اشتراک)
3.  Level Screen: لیست 12 داستان هر سطح
4.  Lesson Screen: محتوای اصلی آموزش
5.  Paywall Screen: صفحه خرید اشتراک کافه بازار

---

### مرحله ۲: اسکلت اپ (کدنویسی صفحات خالی)
من الان اسکلت 3 صفحه اصلی را بدون محتوا می‌سازم. فقط دکمه‌ها کار کنند:
Home -> Level -> Lesson
وقتی طراحی تو نهایی شد، من فقط رنگ و شکل دکمه‌ها را طبق طرح تو عوض می‌کنم.

### مرحله ۳: محتوا (بعد از تایید طراحی)
شروع می‌کنیم A1 را می‌سازیم. هر درس = یک فایل JSON. تو دیگه کد نمی‌زنی، فقط محتوای من را تایید می‌کنی.

### مرحله ۴: کسب درآمد در کافه‌بازار (خیلی مهم)
کافه‌بازار مثل گوگل‌پلی نیست. برای اشتراک باید از سرویس پرداخت کافه‌بازار استفاده کنی.
دو راه داری:
**راه ساده (برای شروع):** اپ را کاملا رایگان در کافه‌بازار بذار، ولی فقط A1 را باز بذار. برای A2 به بالا داخل اپ بنویس "برای خرید اشتراک به پیج اینستا پیام بده" یا یک شماره کارت. خیلی از اپ‌های ایرانی اول همین کار را می‌کنند.
**راه حرفه‌ای:** ما پلاگین `cafebazaar_flutter` را به پروژه اضافه می‌کنیم و پرداخت درون‌برنامه‌ای کافه‌بازار را فعال می‌کنیم. من کدش را برایت می‌نویسم.

### مرحله ۵: انتشار
- ساخت آیکون و نام پکیج (مثلا ir.app.germanstories)
- گرفتن خروجی `aab` برای کافه‌بازار: `flutter build appbundle`
- بارگذاری در پنل کافه‌بازار

---
### الان تو چی کار کن؟

1.  همین پروژه آپدیت شده را در VS Code باز کن و با `flutter run -d chrome` ببین. الان 3 صفحه دارد.
2.  طراحی‌ات را هر وقت آماده شد (عکس، اسکرین‌شات، یا حتی متن) برایم بفرست.
3.  بگو: آیا می‌خواهی الان با همین طراحی ساده ادامه دهیم یا صبر کنم تا طراحی خودت آماده شود؟

من قدم به قدم بهت یاد می‌دهم هر تکه کد چی کار می‌کند.
