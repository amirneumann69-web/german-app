# آموزش قدم به قدم - از صفر تا نمایش درس اول در مرورگر

این آموزش مخصوص توست که اولین بارته. با حوصله بخون.

### قدم 0: چه چیزی داری؟

تو الان یک پوشه به نام `german_story_app` داری که من برات ساختم. داخلش این‌ها هست:

```
german_story_app/
├── lib/  <- کدهای اپ (به این دست نزن)
│   ├── main.dart
│   └── screens/ (3 تا صفحه: خانه، لیست، درس)
├── assets/
│   └── lessons/
│       └── a1_l01.json  <- همین فایل درس اول که الان ساختیم، اینجاست!
├── pubspec.yaml <- تنظیمات
└── content/
    └── A1_L01_Erste_Tage.md <- نسخه خوانای درس برای خودت
```

### قدم 1: VS Code را باز کن

1. VS Code را باز کن
2. از منوی بالا بزن: File -> Open Folder
3. پوشه `german_story_app` را که دانلود کردی انتخاب کن و Open بزن.
4. سمت چپ VS Code یک لیست فایل‌ها می‌بینی. دقیقا مثل عکسی که پایین این راهنما هست.

### قدم 2: فایل JSON درس کجاست و چیست؟

1. در سمت چپ، روی پوشه `assets` کلیک کن تا باز شود.
2. داخلش پوشه `lessons` را باز کن.
3. فایل `a1_l01.json` را می‌بینی. روی آن کلیک کن.

این فایل قلب درس توست! داخلش 6 بخش هست:

```json
{
  "title_de": "Erste Tage in Berlin",
  "story_de": "Hallo! Ich bin Amir...",
  "story_fa": "سلام! من امیر هستم...",
  "vocabulary": [ لیست لغات ],
  "grammar": { توضیح گرامر فارسی },
  "exercises": [ تمرین‌ها ]
}
```

هر چی داخل این فایل بنویسی، داخل اپ نمایش داده می‌شود.

### قدم 3: اون جمله یعنی چی؟ (کپی کنی مستقیم نمایش داده میشه)

من در فایل `lib/screens/lesson_screen.dart` یک کد نوشتم که می‌گوید:

> "برو از پوشه assets/lessons فایل a1_l01.json را بخوان و هر چی داخلش بود را نمایش بده"

این کد اینه:
```dart
final jsonStr = await rootBundle.loadString('assets/lessons/a1_l01.json');
```

پس تو لازم نیست کدنویسی کنی! فقط:
- من فایل JSON رو می‌سازم
- تو اون رو در پوشه `assets/lessons/` کپی می‌کنی
- اپ خودش داستان، ترجمه مخفی، لغات و گرامر رو نمایش می‌ده

برای دکمه ترجمه مخفی هم کدش آماده‌ست:
```dart
ElevatedButton(
  onPressed: () => setState(() => showTranslation = !showTranslation),
  child: Text('نمایش ترجمه فارسی')
)
```
یعنی اول `showTranslation = false` هست و ترجمه مخفی است، وقتی کاربر کلیک کرد `true` می‌شود و نمایش داده می‌شود.

### قدم 4: چطور در مرورگر (کروم) ببینی؟

1. در VS Code از منوی بالا بزن: Terminal -> New Terminal (یک صفحه سیاه پایین باز می‌شود)
2. اول این دستور را بزن و Enter بزن (اینترنت لازم دارد):
```
flutter pub get
```
این دستور می‌گوید کتابخانه‌ها را دانلود کن. یک بار کافیست.

3. بعد این دستور را بزن:
```
flutter run -d chrome
```
صبر کن 30 ثانیه... یک صفحه کروم باز می‌شود و اپ تو بالا می‌آید!

مراحل داخل اپ:
- صفحه اول: لیست سطوح (A1 رایگان)
- روی A1 کلیک کن -> می‌روی صفحه دوم: لیست 12 داستان
- روی داستان 1 (Erste Tage in Berlin) کلیک کن -> می‌روی صفحه سوم: اینجا همان `a1_l01.json` نمایش داده می‌شود!
  - متن آلمانی را می‌بینی
  - دکمه "نمایش ترجمه فارسی" را بزن -> ترجمه فارسی که در JSON بود نمایش داده می‌شود
  - پایین‌تر لیست لغات و گرامر

### قدم 5: اگر خواستی درس دوم را اضافه کنی چی؟

خیلی ساده:

1. فایل `a1_l01.json` را کپی کن و نامش را بگذار `a1_l02.json`
2. داخل `a1_l02.json` متن داستان و لغات درس دوم را بنویس (یا من برات می‌سازم)
3. فایل جدید را در همان پوشه `assets/lessons/` بگذار
4. دوباره دستور `flutter run -d chrome` را بزن.

من برای اینکه کار تو راحت شود، در آینده یک کد می‌نویسم که خودش همه فایل‌های داخل `lessons` را خودکار پیدا کند. فعلا برای درس اول همین یک فایل کافیست.

### مشکلات رایج:

**سوال: زدم flutter run -d chrome خطا داد Asset not found**
جواب: برو فایل `pubspec.yaml` را باز کن. ببین این خط هست یا نه:
```yaml
flutter:
  assets:
    - assets/lessons/
```
اگر نیست، اضافه کن و دوباره `flutter pub get` بزن.

**سوال: فارسی به هم ریخته نمایش داده می‌شود؟**
جواب: در `lib/main.dart` من `Directionality(textDirection: rtl)` گذاشتم تا فارسی درست شود. دست نزن.

**سوال: ترجمه مخفی کار نمی‌کند؟**
جواب: کدش در `lesson_screen.dart` هست. اگر کار نکرد به من بگو تا چک کنم.

---

### خلاصه یک خطی:

تو فقط روی محتوا (JSON) کار می‌کنی، کد نمایش (دکمه مخفی و ...) با من. من JSON را می‌دهم، تو کپی می‌کنی در `assets/lessons/`، بعد `flutter run -d chrome` می‌زنی و نتیجه را در مرورگر می‌بینی.
